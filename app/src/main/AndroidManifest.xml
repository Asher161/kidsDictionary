<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.e.kidstalkingdictionary">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Word"></activity>
        <activity android:name=".ShowWords" />
        <activity
            android:name=".Favorite"
            android:label="Favorites" />
        <activity
            android:name=".RandomSearch"
            android:label="Random Search" />
        <activity
            android:name=".AddWord"
            android:label="Add Words" />
        <activity
            android:name=".SearchWords"
            android:label="Search Words" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>